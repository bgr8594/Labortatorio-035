import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ionic.starter',
  appName: 'ionic-basic-1913852',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
