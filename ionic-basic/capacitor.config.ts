import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.democapacitor.fcfm',
  appName: 'ionic-basic-1170333',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
