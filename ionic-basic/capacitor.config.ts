import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ionics.starter',
  appName: 'ionic-basic-1930502',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
