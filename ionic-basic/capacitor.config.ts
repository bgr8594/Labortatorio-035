import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'como.ionics.starter',
  appName: 'ionic-basic-1865715',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
