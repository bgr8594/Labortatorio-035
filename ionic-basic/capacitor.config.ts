import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.democapacitor.fcfm',
  appName: 'ionic-basic-1865715',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
